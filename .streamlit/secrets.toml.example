# Copy to secrets.toml and fill in real values. This file is safe to commit.
# secrets.toml is gitignored and never committed.

github_token   = "ghp_..."
roic_api_key   = "your_roic_key"
nvidia_api_key = "nvapi-..."
app_password   = "choose_a_strong_password"
csv_enc_key    = "generate_with: python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\""
